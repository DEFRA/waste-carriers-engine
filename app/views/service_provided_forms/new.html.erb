<%= render("shared/back", back_path: back_service_provided_forms_path(@service_provided_form.reg_identifier)) %>

<% if @service_provided_form.errors.any? %>

  <h1 class="heading-large"><%= t(".error_heading") %></h1>

  <% @service_provided_form.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>

<% else %>

  <%= form_for(@service_provided_form) do |f| %>
    <%= render("shared/errors", object: @service_provided_form) %>

    <h1 class="heading-large"><%= t(".heading") %></h1>

    <%= f.hidden_field :reg_identifier, value: @service_provided_form.reg_identifier %>
    <div class="form-group">
      <%= f.submit t(".next_button"), class: "button" %>
    </div>
  <% end %>

<% end %>
